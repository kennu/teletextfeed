<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :next_number %><br />
    <%= f.text_field :next_number %>
  </div>
  <div class="field">
    <%= f.label :prev_number %><br />
    <%= f.text_field :prev_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
